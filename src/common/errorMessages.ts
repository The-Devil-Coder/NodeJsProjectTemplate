import {httpStatusCode} from './enum'

export const errors = {
    "BadRequestException": {
        statusCode: httpStatusCode.BAD_REQUEST,
        message: "",
        errorCode: "BAD_REQUEST"        
    }
}